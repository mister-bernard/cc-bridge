{
  "name": "cc-bridge",
  "version": "0.3.0",
  "private": true,
  "description": "Persistent Claude Code sessions exposed as an OpenAI-compatible /v1/chat/completions endpoint. Multi-session bridge for Telegram, Signal, HTTP, and cron-driven workflows.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mister-bernard/cc-bridge.git"
  },
  "type": "module",
  "main": "src/daemon.js",
  "scripts": {
    "start": "node src/daemon.js",
    "test": "node --test 'tests/unit/*.test.js' 'tests/security/*.test.js'"
  },
  "engines": {
    "node": ">=18"
  }
}
