{
  "name": "cc-bridge",
  "version": "0.3.0",
  "private": true,
  "description": "Persistent Claude Code sessions exposed as an OpenAI-compatible provider. Multi-session bridge for Telegram, Signal, HTTP, and crons.",
  "type": "module",
  "main": "src/daemon.js",
  "scripts": {
    "start": "node src/daemon.js",
    "test": "node --test 'tests/unit/*.test.js' 'tests/security/*.test.js'"
  },
  "engines": {
    "node": ">=18"
  }
}
